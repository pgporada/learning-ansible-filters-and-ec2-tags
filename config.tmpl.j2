some_key: "myvalue"
retry_join: [{% for h in consul_servers %}"{{ h }}"{% if not loop.last %},{% endif %}{% endfor %}]
start_join: [{% for h in consul_servers %}"{{ h }}"{% if not loop.last %},{% endif %}{% endfor %}]
bootstrap_expect: {{ consul_servers | length }}
